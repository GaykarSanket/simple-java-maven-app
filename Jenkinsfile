pipeline {
    agent { 
        label 'docker-slave'
    }
    stages {
        stage('Build') {
            steps {
                sh 'echo 'c' | ssh test@10.71.71.168'
            }
        }
        
    }
}

